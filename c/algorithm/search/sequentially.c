#include <stdio.h>
#define SIZE 10

//テスト検索データ
int target = 23;
// テストデータ
int data[SIZE] = {4,5,8,23,25,35,39,49,50,59,95};

void main(void) {
  int size = SIZE;
  int *search;
  search = data[0];
  while (target == i) {
    size = (size + size % 2) / 2;
    if (target > data[size]) {
      search = data[size + 1];
    } else if (target < data[size]) {
      
    }
  }
}

